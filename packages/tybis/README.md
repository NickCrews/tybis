# Core tybis package

This is the core tybis package.
It provides the operations, expressions, datatypes, relations, schemas, etc.
It has no dependencies.

It does NOT have any of the backend-specific stuff,
such as a PRQL compiler, SQL compiler, etc.
Those are in the sibling packages.
